#include <vector>
/* Factorization functions */
extern int gcd(int a, int b);
extern int trivial_factor(int n);
extern void factorize(std::vector<int> & factors, int n);
extern int factor(int);
/* ----------------------- */

